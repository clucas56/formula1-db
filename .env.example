# Copy this file to `.env` and fill in real values.
# `.env` is gitignored — never commit real secrets.

# PostgreSQL
DB_NAME=f1_data
DB_USER=f1user
DB_PASSWORD=change-me-to-a-strong-password

# Ingestion schedule (cron syntax) — default Monday 6am
INGESTION_CRON=0 6 * * 1
